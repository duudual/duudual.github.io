# 个性化设置指南 - 代码修改位置

## 📁 主要配置文件（必须修改）

### 1. `_config.yml` - 网站主配置
**位置：** 项目根目录

这是最重要的配置文件，修改以下内容：

```yaml
# 网站基本信息
title: "你的网站标题"
subtitle: "网站副标题"
name: "你的名字"
description: "网站描述"

# 网站URL（GitHub Pages）
url: "https://你的用户名.github.io"
baseurl: ""
repository: "你的用户名/仓库名"

# 主题皮肤（9种可选）
minimal_mistakes_skin: "dark"  # default, air, aqua, contrast, dark, dirt, neon, mint, plum, sunrise

# 作者信息
author:
  name: "你的名字"
  avatar: "/assets/images/bio-photo.jpg"
  bio: "你的个人简介"
  location: "你的位置"
  email: "your.email@example.com"
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/你的用户名"
    - label: "Twitter"
      icon: "fab fa-fw fa-twitter-square"
      url: "https://twitter.com/你的用户名"

# 搜索功能
search: true
search_provider: lunr

# 评论功能（可选）
comments:
  provider: "giscus"  # 或 "disqus", "utterances" 等
  giscus:
    repo_id: "你的仓库ID"
    category_name: "Announcements"
    category_id: "你的分类ID"
    theme: "light"
```

---

## 📁 导航和UI文本配置

### 2. `_data/navigation.yml` - 导航菜单
**位置：** `_data/navigation.yml`

修改导航菜单链接：

```yaml
main:
  - title: "首页"
    url: /
  - title: "关于"
    url: /about/
  - title: "博客"
    url: /year-archive/
  - title: "作品集"
    url: /portfolio-archive/
  - title: "联系"
    url: /contact/
```

### 3. `_data/ui-text.yml` - 界面文本（可选）
**位置：** `_data/ui-text.yml`

自定义界面文本，支持中文。可以修改按钮文字、标签等。

---

## 🎨 样式自定义

### 4. `assets/css/main.scss` - 自定义CSS
**位置：** `assets/css/main.scss`

在这里添加你的自定义样式：

```scss
---
# 只让 Jekyll 读取这个文件
---

@charset "utf-8";

@import "minimal-mistakes/skins/{{ site.minimal_mistakes_skin | default: 'default' }}";
@import "minimal-mistakes";

// ===== 你的自定义样式从这里开始 =====

// 修改网站标题样式
.site-title {
  font-size: 2.5em;
  font-weight: bold;
  color: #your-color;
}

// 修改链接颜色
a {
  color: #your-link-color;
  &:hover {
    color: #your-hover-color;
  }
}

// 修改侧边栏样式
.sidebar {
  background-color: #your-bg-color;
}

// 修改按钮样式
.btn {
  border-radius: 5px;
  padding: 10px 20px;
}
```

### 5. `_sass/minimal-mistakes/_variables.scss` - 主题变量（高级）
**位置：** `_sass/minimal-mistakes/_variables.scss`

如果你想深度自定义，可以修改主题的SCSS变量（颜色、字体、间距等）。

**注意：** 由于你fork了整个主题，可以直接修改这个文件。如果使用gem版本，需要先复制到项目中。

---

## 🧩 布局和组件自定义

### 6. `_layouts/` - 页面布局
**位置：** `_layouts/`

可以修改的布局文件：
- `home.html` - 首页布局
- `single.html` - 单页/文章布局
- `archive.html` - 归档页布局
- `default.html` - 默认布局

**示例：** 修改 `_layouts/single.html` 来改变文章页面的结构。

### 7. `_includes/` - 可复用组件
**位置：** `_includes/`

可以修改的组件：
- `masthead.html` - 顶部导航栏
- `footer.html` - 页脚
- `author-profile.html` - 作者信息侧边栏
- `sidebar.html` - 侧边栏
- `head/custom.html` - 自定义head内容（添加CSS、JS等）
- `footer/custom.html` - 自定义footer内容

**示例：** 在 `_includes/head/custom.html` 中添加自定义CSS：

```html
<!-- 添加自定义CSS -->
<link rel="stylesheet" href="/assets/css/custom.css">

<!-- 或直接写样式 -->
<style>
  .my-custom-class {
    color: red;
  }
</style>
```

**示例：** 在 `_includes/footer/custom.html` 中添加自定义脚本：

```html
<!-- 添加自定义JavaScript -->
<script src="/assets/js/custom.js"></script>

<!-- 或直接写脚本 -->
<script>
  console.log('Custom footer script');
</script>
```

---

## 📄 页面和内容

### 8. `index.html` - 首页
**位置：** 项目根目录

修改首页内容：

```html
---
layout: home
author_profile: true
---

<!-- 你可以在这里添加自定义内容 -->
<div class="custom-home-content">
  <h1>欢迎来到我的网站！</h1>
  <p>这是我的个人主页...</p>
</div>
```

### 9. `_pages/` - 自定义页面
**位置：** 创建 `_pages/` 文件夹（如果不存在）

创建自定义页面，例如：
- `_pages/about.md` - 关于页面
- `_pages/contact.md` - 联系页面
- `_pages/portfolio.md` - 作品集页面

**示例：** `_pages/about.md`

```markdown
---
layout: single
title: "关于我"
permalink: /about/
author_profile: true
---

这是我的关于页面内容...
```

### 10. `_posts/` - 博客文章
**位置：** 创建 `_posts/` 文件夹（如果不存在）

创建博客文章，文件名格式：`YYYY-MM-DD-文章标题.md`

**示例：** `_posts/2024-01-01-welcome.md`

```markdown
---
layout: single
title: "欢迎来到我的博客"
date: 2024-01-01
categories:
  - 博客
tags:
  - 欢迎
---

这是文章内容...
```

---

## 🖼️ 静态资源

### 11. `assets/images/` - 图片
**位置：** `assets/images/`

放置你的图片文件：
- 头像：`bio-photo.jpg`
- Logo：`logo.png`
- 文章配图等

### 12. `assets/js/` - JavaScript
**位置：** `assets/js/`

添加自定义JavaScript文件。

---

## 🔧 高级自定义

### 13. 覆盖主题默认文件

由于你fork了整个主题，你可以直接修改以下文件夹中的任何文件：
- `_layouts/` - 布局文件
- `_includes/` - 组件文件
- `_sass/` - 样式文件
- `assets/` - 静态资源

**重要提示：**
- 直接修改这些文件会影响整个主题
- 建议先备份，或者使用Git版本控制
- 如果将来要更新主题，你的修改可能会被覆盖

### 14. 使用自定义文件覆盖

如果你想保留主题原文件，可以创建同名文件来覆盖：

例如，要自定义侧边栏：
1. 复制 `_includes/sidebar.html` 到你的项目
2. 修改这个副本
3. Jekyll会优先使用你项目中的文件

---

## 📝 快速修改清单

### 基础个性化（必须）
- [ ] 修改 `_config.yml` 中的网站信息
- [ ] 修改 `_data/navigation.yml` 导航菜单
- [ ] 创建 `_pages/about.md` 关于页面
- [ ] 创建 `_posts/` 文件夹并添加文章

### 样式个性化（可选）
- [ ] 在 `assets/css/main.scss` 添加自定义CSS
- [ ] 修改 `_sass/minimal-mistakes/_variables.scss` 主题变量
- [ ] 在 `_includes/head/custom.html` 添加自定义样式

### 功能个性化（可选）
- [ ] 修改 `_includes/masthead.html` 顶部导航
- [ ] 修改 `_includes/footer.html` 页脚
- [ ] 修改 `_includes/author-profile.html` 作者信息

### 内容个性化（必须）
- [ ] 添加头像到 `assets/images/bio-photo.jpg`
- [ ] 添加Logo到 `assets/images/logo.png`
- [ ] 创建博客文章和页面

---

## 🎯 常用修改示例

### 示例1：修改网站标题颜色

在 `assets/css/main.scss` 中添加：

```scss
.site-title {
  color: #ff6b6b !important;
}
```

### 示例2：添加自定义导航链接

在 `_data/navigation.yml` 中：

```yaml
main:
  - title: "首页"
    url: /
  - title: "博客"
    url: /year-archive/
  - title: "项目"
    url: /portfolio/
```

### 示例3：修改作者信息

在 `_config.yml` 中：

```yaml
author:
  name: "你的名字"
  avatar: "/assets/images/bio-photo.jpg"
  bio: "这是我的个人简介，支持**Markdown**格式。"
  location: "北京, 中国"
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/你的用户名"
    - label: "邮箱"
      icon: "fas fa-fw fa-envelope-square"
      url: "mailto:your.email@example.com"
```

### 示例4：添加自定义CSS文件

1. 创建 `assets/css/custom.css`
2. 在 `_includes/head/custom.html` 中添加：

```html
<link rel="stylesheet" href="{{ '/assets/css/custom.css' | relative_url }}">
```

### 示例5：修改主题皮肤

在 `_config.yml` 中：

```yaml
minimal_mistakes_skin: "dark"  # 改为深色主题
```

可选值：`default`, `air`, `aqua`, `contrast`, `dark`, `dirt`, `neon`, `mint`, `plum`, `sunrise`

---

## ⚠️ 注意事项

1. **修改 `_config.yml` 后需要重启Jekyll服务器**
2. **图片路径使用完整路径**，如：`/assets/images/photo.jpg`
3. **使用Git版本控制**，方便回退修改
4. **先本地测试**，确认无误后再推送到GitHub
5. **备份重要文件**，特别是自定义的布局和组件

---

## 🚀 开始修改

1. **先修改基础配置**：`_config.yml` 和 `_data/navigation.yml`
2. **添加内容**：创建 `_pages/` 和 `_posts/` 文件夹
3. **自定义样式**：在 `assets/css/main.scss` 添加CSS
4. **本地测试**：运行 `bundle exec jekyll serve`
5. **推送到GitHub**：确认无误后推送

祝你个性化设置顺利！🎉

