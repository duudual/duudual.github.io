# Minimal Mistakes 主题使用指南

## 项目概述

这是一个 **Minimal Mistakes** Jekyll 主题项目，这是一个灵活的双栏 Jekyll 主题，非常适合构建个人网站、博客和作品集。

## 如何使用这个项目美化你的网页

### 第一步：安装和配置

#### 1. 配置 _config.yml 文件

这是最重要的配置文件，你需要修改以下关键设置：

```yaml
# 站点基本信息
title: "你的网站标题"                    # 网站标题
subtitle: "你的网站副标题"                # 网站副标题（可选）
name: "你的名字"                         # 作者名字
description: "你的网站描述"               # 网站描述，用于SEO

# 网站URL（GitHub Pages）
url: "https://你的用户名.github.io"      # 你的GitHub Pages URL
baseurl: ""                              # 如果是项目页面，填写"/项目名"
repository: "你的用户名/仓库名"            # GitHub仓库

# 主题皮肤
minimal_mistakes_skin: "default"         # 可选: "air", "aqua", "contrast", "dark", "dirt", "neon", "mint", "plum", "sunrise"

# 作者信息
author:
  name: "你的名字"
  avatar: "/assets/images/bio-photo.jpg"  # 头像路径
  bio: "你的个人简介"
  location: "你的位置"
  email: "your.email@example.com"
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/你的用户名"
    - label: "Twitter"
      icon: "fab fa-fw fa-twitter-square"
      url: "https://twitter.com/你的用户名"

# 导航菜单
# 在 _data/navigation.yml 中配置
```

#### 2. 配置导航菜单

编辑 `_data/navigation.yml` 文件：

```yaml
main:
  - title: "关于"
    url: /about/
  - title: "博客"
    url: /year-archive/
  - title: "作品集"
    url: /portfolio-archive/
  - title: "联系"
    url: /contact/
```

#### 3. 配置 UI 文本（可选，支持中文）

编辑 `_data/ui-text.yml` 文件，可以自定义界面文本。主题已经支持中文，但你可以根据需要调整。

### 第二步：创建内容

#### 1. 创建首页

编辑 `index.html`：

```html
---
layout: home
author_profile: true
---
```

#### 2. 创建页面

在 `_pages` 文件夹中创建页面（如果没有这个文件夹，需要创建）：

**示例：关于页面** (`_pages/about.md`)

```markdown
---
layout: single
title: "关于"
permalink: /about/
author_profile: true
---

这是关于页面的内容。
```

#### 3. 创建博客文章

在 `_posts` 文件夹中创建文章：

**示例：** (`_posts/2024-01-01-welcome.md`)

```markdown
---
layout: single
title: "欢迎来到我的博客"
date: 2024-01-01
categories:
  - 博客
tags:
  - 欢迎
---

这是你的第一篇博客文章！
```

### 第三步：自定义样式（可选）

#### 1. 选择主题皮肤

在 `_config.yml` 中修改：

```yaml
minimal_mistakes_skin: "dark"  # 例如：使用深色主题
```

可用的皮肤：
- `default` - 默认（浅色）
- `air` - 蓝色清新
- `aqua` - 水蓝色
- `contrast` - 高对比度
- `dark` - 深色
- `dirt` - 大地色
- `neon` - 霓虹色
- `mint` - 薄荷绿
- `plum` - 紫色
- `sunrise` - 日出橙红色

#### 2. 添加自定义 CSS

创建 `assets/css/main.scss` 文件（如果不存在），添加：

```scss
---
# 只让 Jekyll 读取这个文件
---

@import "minimal-mistakes";

// 你的自定义样式
.site-title {
  font-size: 2em;
}
```

### 第四步：添加功能

#### 1. 启用搜索功能

在 `_config.yml` 中添加：

```yaml
search: true
search_provider: lunr  # 或 "algolia", "google"
```

#### 2. 启用评论功能

在 `_config.yml` 中配置（示例：使用 Giscus）：

```yaml
repository: "你的用户名/仓库名"
comments:
  provider: "giscus"
  giscus:
    repo_id: "你的仓库ID"
    category_name: "Announcements"
    category_id: "你的分类ID"
    theme: "light"
```

#### 3. 添加社交媒体链接

在 `_config.yml` 中：

```yaml
footer:
  links:
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/你的用户名"
    - label: "Twitter"
      icon: "fab fa-fw fa-twitter-square"
      url: "https://twitter.com/你的用户名"
```

#### 4. 添加 Google Analytics（可选）

```yaml
analytics:
  provider: "google-gtag"
  google:
    tracking_id: "UA-XXXXXXXXX-X"
```

### 第五步：本地测试

#### 1. 安装依赖

```bash
bundle install
```

#### 2. 启动本地服务器

```bash
bundle exec jekyll serve
```

访问 `http://localhost:4000` 查看你的网站。

### 第六步：部署到 GitHub Pages

#### 方法一：使用 Remote Theme（推荐）

1. 在 `_config.yml` 中设置：

```yaml
remote_theme: "mmistakes/minimal-mistakes@4.27.3"
```

2. 确保 `Gemfile` 包含：

```ruby
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins
```

3. 推送到 GitHub：

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

#### 方法二：直接使用 Fork 的仓库

如果你已经 fork 了这个仓库：

1. 重命名仓库为 `你的用户名.github.io`
2. 修改 `_config.yml` 配置
3. 推送到 GitHub
4. 在仓库设置中启用 GitHub Pages

### 重要提示

1. **删除不需要的文件**：如果你 fork 了这个仓库，可以删除：
   - `/docs` 文件夹（文档）
   - `/test` 文件夹（测试文件）
   - `README.md`（如果你想用自己的）
   - `CHANGELOG.md`
   - `screenshot*.png`

2. **配置文件**：修改 `_config.yml` 后需要重启 Jekyll 服务器才能生效。

3. **图片路径**：所有图片应该放在 `/assets/images/` 目录下，使用完整路径，如：`/assets/images/my-image.jpg`

4. **中文支持**：主题已经支持中文，在 `_config.yml` 中设置：

```yaml
locale: "zh-CN"
```

### 常用布局

- `home` - 首页布局
- `single` - 单页布局（文章、页面）
- `archive` - 归档布局
- `splash` - 启动页布局
- `search` - 搜索页布局

### 获取帮助

- 官方文档：https://mmistakes.github.io/minimal-mistakes/docs/quick-start-guide/
- GitHub Issues：https://github.com/mmistakes/minimal-mistakes/issues

## 快速开始清单

- [ ] 修改 `_config.yml` 中的基本信息
- [ ] 配置 `_data/navigation.yml` 导航菜单
- [ ] 创建 `_pages` 文件夹并添加页面
- [ ] 创建 `_posts` 文件夹并添加文章
- [ ] 添加你的头像到 `/assets/images/`
- [ ] 选择主题皮肤
- [ ] 本地测试（`bundle exec jekyll serve`）
- [ ] 推送到 GitHub
- [ ] 启用 GitHub Pages

祝你使用愉快！🎉

